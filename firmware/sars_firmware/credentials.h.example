// WiFi credentials for SARS Posture Monitor
// Copy this file to credentials.h and fill in your values:
//   cp credentials.h.example credentials.h
//
// credentials.h is gitignored and will not be committed.

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASS = "YOUR_WIFI_PASSWORD";

// Fallback Access Point (created when WiFi connection fails)
const char* AP_SSID = "SARS-Kamera";
const char* AP_PASS = "sars1234";

// API key for authenticated endpoints (state, config, buzzer)
// Leave empty to disable auth (backward compatible)
const char* API_KEY = "";

#endif
